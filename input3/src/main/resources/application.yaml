server:
  port: 9094
  tomcat:
    accesslog:
      encoding: UTF-8
    uri-encoding: UTF-8
    threads:
      max: 300
      min-spare: 30

spring:
  application:
    name: input3

  threads:
    virtual:
      enabled: true # 虚拟线程

  profiles:
    active: dev # 配置文件
  devtools:
    livereload:
      enabled: false

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl